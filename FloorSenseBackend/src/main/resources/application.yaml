spring:
  application:
    name: FloorSenseBackend

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://mongodb:27017/floorsense}

  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        project-id: floorsense
        location: us-central1
        chat:
          options:
            model: gemini-2.0-flash